<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.iguest.quizdroidpart1" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MathActivity"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".MathQuestionActivity"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".PhysicsActivity"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".PhysicsQuestionActivity"
            android:label="@string/app_name">
        </activity>

        <activity
        android:name=".MarvelActivity"
        android:label="@string/app_name">
    </activity>

        <activity
            android:name=".MarvelQuestionActivity"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".AnswerActivity"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".PhysicsAnswerActivity"
            android:label="@string/app_name">
        </activity>



    </application>

</manifest>
